apiVersion: v1
kind: Service
metadata:
  name: {{ include "friendly-octo-guacamole.fullname" . }}
  labels:
    {{- include "friendly-octo-guacamole.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "friendly-octo-guacamole.selectorLabels" . | nindent 4 }}
